let tasks = []
let completedTasks =[]

createElement(type, parentEl);
function createElement() {
    let element = document.createElement(type);
    // document.body.appendChild(input);
    document.parentEl.appendChild(element)
}

createElement("input", body)

